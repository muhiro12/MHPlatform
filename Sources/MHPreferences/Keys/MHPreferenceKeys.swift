/// A concrete namespace root for app-owned preference keys.
public struct MHPreferenceKeys: Sendable {
    /// Creates a preference-key namespace root.
    public init() {
        // Apps extend this namespace with computed descriptor properties.
    }
}
